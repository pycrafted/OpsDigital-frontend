import React from 'react';

/**
 * Page « Feuille de saisie » (tableaux) — contenu vide pour l’instant.
 * Ouverte depuis l’icône du footer sur la page tableaux.
 */
const FeuilleSaisieVide: React.FC = () => {
  return <></>;
};

export default FeuilleSaisieVide;
